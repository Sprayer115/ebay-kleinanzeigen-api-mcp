# Server deployment environment configuration
# Copy this to .env.server and fill in your values

# MCP Server Configuration
# API Key for authentication (generate with: openssl rand -hex 32)
MCP_API_KEY=your-secure-api-key-here

# Port to expose (default: 8000)
# Your reverse proxy (Nginx, Traefik, Caddy) should forward to this port
MCP_PORT=8000

# Logging Level
LOG_LEVEL=INFO

# Monitoring (optional, only if using --profile monitoring)
# Prometheus port (default: 9090)
PROMETHEUS_PORT=9090

# Grafana configuration
GRAFANA_PORT=3000
GRAFANA_PASSWORD=your-grafana-password
GRAFANA_URL=http://localhost:3000

# Resource Limits (adjust based on your server)
# MAX_CONCURRENT_REQUESTS=50
# REQUEST_TIMEOUT=30
