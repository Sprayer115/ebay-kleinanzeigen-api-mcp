# Server deployment environment configuration
# Copy this to .env.server and fill in your values

# Domain Configuration
# Your server's domain name (must have DNS A record pointing to your server IP)
MCP_DOMAIN=mcp.example.com

# Let's Encrypt Configuration
# Email for Let's Encrypt certificate notifications
ACME_EMAIL=admin@example.com

# Authentication
# Generate a strong random API key: openssl rand -hex 32
MCP_API_KEY=your-secure-api-key-here

# Monitoring (optional, only if using --profile monitoring)
# Password for Grafana admin user
GRAFANA_PASSWORD=your-grafana-password

# Traefik Dashboard (optional)
# Basic auth user:password (generate with: htpasswd -nb admin password)
# TRAEFIK_AUTH=admin:$apr1$...

# Server Configuration
# Uncomment and adjust if needed
# HOST=0.0.0.0
# PORT=8000

# Logging Level
# LOG_LEVEL=INFO

# Resource Limits (adjust based on your server)
# MAX_CONCURRENT_REQUESTS=50
# REQUEST_TIMEOUT=30
